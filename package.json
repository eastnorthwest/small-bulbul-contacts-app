{
  "name": "week5_contacts_app",
  "version": "1.0.0",
  "description": "Build a contacts manager web application with Express and PostgreSQL.",
  "scripts": {
    "schema": "psql < schema.sql",
    "import": "psql < contacts_full.sql",
    "start": "node server.js",
    "start:dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "address-validator": "^0.2.2",
    "body-parser": "^1.17.2",
    "ejs": "^2.5.6",
    "express": "^4.15.3",
    "moment": "^2.18.1",
    "nodemon": "^1.11.0",
    "pg": "^6.2.3"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  },
  "engines": {
    "node": "4.7.2"
  }
}
